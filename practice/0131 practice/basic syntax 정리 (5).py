'''
[ 함수 ]

- 뭐 위치>기본>키워드인자 순
- 함수명 (동사) 로 활용해서 지어야하고

매개변수 = 함수 내부 전용 변수 
한 마디로 함수 식에서 사용하는 임시 변수라고 볼 수 있음
함수의 코드 실행 - 단락이 끝나면 해당 매개변수의 변수는 메모리에서 사라짐
함수 외부의 데이터를 함수 내부로 전달하기 위한 입구 변수라고 볼 수 있음

- return이 없다는 것
:함수가 종료될 때 호출한 위치로 아무 값을 보내지 않는다는 뜻
함수로서의 기능은 하나 이후 코드에게 결과 데이터 주지 X

매개변수 역할 : 외부 데이터를 안으로 넣기
return 값 : 안의 데이터를 밖으로 빼기

패킹 (함수 정의시) 
- * : 매개변수 앞에 붙임. 남는 위치인자들 튜플로 묶기. 
- ** : 매개변수 앞에 붙임. 남는 키워드 인자들 딕셔너리로 묶기.

언패킹 (함수 호출할 때)
- * : 리스트나 튜플 앞에 붙임. 각 요소를 함수 개별 위치 인자로 전달
- ** : 딕셔너리 앞에 붙임. 키 값이 매개변수라 치면 VALUE 값을 키워드 인자로 전달. 


1. 함수는 언제나 단 하나의 값 (객체)만 반환 가능
***여러 값을 반환할 때도 하나의 튜플로 packing하여 반환

ex) name, age있으면 return name, age => 하나의 튜플

user data = get_user_info()
print(user_data) # (Alice, 30)
 
2. return a, b, c => 하나의 튜플로 자동 패킹

3. 반환된 튜플 => 각 변수에 unpackking

'''

# 별표(Unpacking Operator, *) 활용
# 만약 리스트를 합쳐서 변수에 담을 필요 없이 출력만 깔끔하게 하고 싶다면 별표(*)를 쓰는 방법이 가장 쉽습니다.

# ex)
# # 리스트 앞에 *를 붙이면 요소들을 하나씩 꺼내줍니다.
# # sep=""은 요소들 사이의 공백을 없애라는 뜻입니다.
# print(*new_text, sep="")


'''
리스트 형태를 다시 문자열로 합치기

ex)

text = input()
new_text = list(text)

new_text[1] = 'a'
new_text[-2] = 'a'

# 리스트 요소들을 빈 문자열("")을 사이에 두고 하나로 합칩니다.
result = "".join(new_text)
print(result)
'''

# Q. elif 구조

# : 순서가 중요. 만약 if 조건도 맞고 elif 조건도 맞는 상황이라 하더라도, 
# 파이썬은 가장 위에 있는 if만 실행하고 나머지는 쳐다도 안 보고 지나가 버린다.

# Q. if만 여러 개 쓰는 거랑 뭐가 다른가? 

# if - elif - else  :   여러 개 중 무조건 하나만 실행메뉴판에서 딱 하나만 골라야 하는 상황
# if - if - if    :   조건만 맞으면 전부 다 실행 가능뷔페에서 먹고 싶은 걸 여러 개 담는 상황

''''''
'''
[ count ] 는 대체 언제 쓰는가? 

1. 필터링: "조건에 맞는 게 총 몇 개야?"
가장 흔한 경우입니다. 데이터 뭉치(리스트나 범위)를 훑으면서 내가 원하는 특정 조건이 나타날 때마다 표시를 하는 겁니다.

상황: 1부터 100까지 숫자 중 짝수가 몇 개인지 셀 때.

논리: if 문이 "참"일 때만 count += 1을 실행해서, 조건에 맞는 사건의 횟수를 기록합니다.

2. 순서 파악: "지금 내가 몇 번째 칸에 있지?"
반복문이 돌아가고 있는 현재의 위치를 숫자로 저장해둬야 할 때 씁니다.

상황: 리스트의 요소들을 출력하면서 앞에 1., 2., 3. 같은 번호를 붙이고 싶을 때.

논리: 반복문이 한 바퀴 돌 때마다 조건과 상관없이 무조건 count += 1을 해서 현재가 몇 번째 루프인지 기록합니다.

3. 상태 변화 감지: "이런 일이 몇 번이나 일어났어?"
단순한 개수가 아니라, 프로그램이 실행되는 동안 특정 사건의 누적 횟수가 중요할 때 씁니다.

상황: 사용자가 비밀번호를 5번 틀리면 접속을 차단해야 할 때.

논리: if (비밀번호 틀림): 조건 안에서 count += 1을 하고, if count >= 5:가 되는 순간 break를 걸어버립니다.


[ count를 써야 할지 판단하는 기준 ]

코드를 짜다가 스스로에게 이렇게 물어보세요.

"나중에(반복문이 다 끝나고 나서) 그게 총 몇 번이었는지 숫자로 대답해야 하나?"

만약 대답이 **"YES"**라면, 무조건 count = 0을 반복문 시작 전에 선언하고 시작해야 합니다.

예시 비교
count 안 쓰는 경우: "10보다 작은 숫자를 다 출력해라." (그냥 print만 하고 지나가면 됨)

count 쓰는 경우: "10보다 작은 숫자가 몇 개인지 출력해라." (기억해둘 count가 필요함)

'''